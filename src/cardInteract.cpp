#include <iostream>
#include <iomanip>
#include "cardInteract.h"

cardInteract :: cardInteract()
{}

bool cardInteract :: moveCard(const bool cardPresent)
{}
bool cardInteract :: noCard(const bool cardPresent)
{}
bool cardInteract ::cardFlip(const bool noFace)
{}

void cardInteract :: print() const{}
